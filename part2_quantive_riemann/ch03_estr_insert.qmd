### II-3.1.2 EONIAから€STRへ

ユーロ市場では、OISの翌日物参照金利としてEONIA（Euro Overnight Index Average）が長く用いられてきた。世界金融危機後、担保付デリバティブの評価にOISカーブを用いる実務が定着すると、EONIAを基礎とするOISカーブは、ユーロ建てデリバティブのディスカウントカーブとして重要な役割を担うようになった。

一方、LIBORをはじめとする金利指標改革は、ユーロの翌日物金利にも及んだ。EONIAから、より広範な実取引に基づく翌日物金利である€STR（Euro Short-Term Rate）への移行が進められ、現在のユーロOIS市場では€STRが中心的な基準金利となっている。

したがって、本章ではEONIAを「金融危機後にOIS discountingが定着した時代を理解するための歴史的ベンチマーク」、€STRを「現在のユーロOIS市場を表す基準金利」として位置づける。後半のケーススタディでは2012年のEONIA市場データを用いて当時のOISカーブ構築を再現し、ここでは最新版GaussQuantを用いて現在の€STRカーブを確認する。

```{r}
#| label: chapter03-estr-current-curve
#| fig-width: 9
#| fig-height: 5.5

estr_benchmark <- GaussQuant::estr_curve_benchmark_GQL()

tbl_estr_curve <- estr_benchmark$validation$quote_repricing |>
  dplyr::select(
    quote_id,
    market_quote,
    implied_quote,
    quote_error,
    pillar_date,
    discount_factor,
    zero_rate,
    inst_fwd_rate
  ) |>
  dplyr::filter(!is.na(pillar_date)) |>
  dplyr::arrange(pillar_date)

GaussQuant::show_tbl_GQH(
  tbl_estr_curve,
  "€STR OIS curve validation",
  n = 20
)

estr_curve_plot <- tbl_estr_curve |>
  dplyr::filter(!is.na(zero_rate)) |>
  ggplot2::ggplot(
    ggplot2::aes(
      x = pillar_date,
      y = 100 * zero_rate
    )
  ) +
  ggplot2::geom_line(linewidth = 0.7) +
  ggplot2::geom_point(size = 1.5) +
  ggplot2::scale_x_date(
    date_breaks = "5 years",
    date_labels = "%Y"
  ) +
  ggplot2::labs(
    title = "€STR OISスポットカーブ",
    x = "満期",
    y = "スポットレート（%）"
  )

show_plot_LFL(estr_curve_plot)
```

ここで表示する€STRカーブと、後半で扱う2012年のEONIAカーブは観測時点が異なるため、金利水準そのものを直接比較するものではない。両者を通じて確認するのは、ユーロOIS市場の基準金利がEONIAから€STRへ移行しても、市場クォートから期間構造を構築し、OIS評価の基礎となるディスカウントカーブを得るという基本的な枠組みが引き継がれていることである。

### II-3.1.3 OISと翌日物金利
